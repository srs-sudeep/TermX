/**
 * src/config/index.ts
 * Re-exports all config objects.
 * No logic here — data only.
 */

export { userConfig } from './user.config';
export { themes } from './themes.config';
export { customCommands } from './commands.config';
export { settings } from './settings.config';
