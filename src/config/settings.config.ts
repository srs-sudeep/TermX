import type { Settings } from '@/types';

export const settings = {
  defaultTheme: 'tokyo-night',

  defaultFontSize: 'md',

  bootSequence: true,

  showBanner: true,

  typewriter: false,
} satisfies Settings;
